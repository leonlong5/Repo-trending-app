import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { AppStackNavigator, AppTabNavigator } from './js/common/AppNavigators'
//use navigator as the app entry
export default AppTabNavigator